// This is just a test to make sure I have webpack/babel configured correctly
// so I may use ES2015+ in my project

const sayHello = (name) => console.log(`hello, ${name}`);

export default sayHello;